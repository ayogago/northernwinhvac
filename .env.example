# Google Workspace SMTP Configuration
# For Google Workspace, you need to create an App Password:
# 1. Go to Google Admin Console
# 2. Navigate to Security > Basic settings
# 3. Under "Less secure app access", enable it OR
# 4. Better: Use App Passwords (requires 2FA enabled)
#    - Go to myaccount.google.com > Security > 2-Step Verification > App Passwords
#    - Generate an app password for "Mail" on "Other" device
#    - Use that 16-character password below

SMTP_USER=info@northernwindhvac.com
SMTP_PASSWORD=your-app-password-here

# Optional: Send confirmation email to customer
SEND_CONFIRMATION_EMAIL=true
