# Google Workspace SMTP Configuration
# For Google Workspace, you need to create an App Password:
# 1. Go to myaccount.google.com > Security
# 2. Enable 2-Step Verification if not already enabled
# 3. Go to App Passwords (under "Signing in to Google")
# 4. Generate an app password for "Mail" on "Other" device
# 5. Use that 16-character password below

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=info@northernwindhvac.com
SMTP_PASSWORD=your-app-password-here

# Contact email (where form submissions go)
CONTACT_EMAIL=info@northernwindhvac.com

# Optional: Send confirmation email to customer
SEND_CONFIRMATION_EMAIL=true
